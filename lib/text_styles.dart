import 'package:flutter/material.dart';

class AppTextStyles {
  static const header = const TextStyle(debugLabel: 'header', fontSize: 22.0);
  static const title = const TextStyle(debugLabel: 'title', fontSize: 18.0);
  static const main = const TextStyle(debugLabel: 'main', fontSize: 14.0);
  static const caption = const TextStyle(debugLabel: 'caption', fontSize: 11.0);
}
